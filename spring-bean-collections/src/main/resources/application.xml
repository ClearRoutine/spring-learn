<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xsi:schemaLocation="
        http://www.springframework.org/schema/beans
        https://www.springframework.org/schema/beans/spring-beans.xsd">

    <bean id="complexObject" class="com.tt.ComplexObject">
        <!-- 1. props → Properties -->
        <property name="adminEmails">
            <props>
                <prop key="administrator">administrator@example.org</prop>
                <prop key="support">support@example.org</prop>
                <prop key="development">development@example.org</prop>
            </props>
        </property>

        <!-- 2. list → List -->
        <property name="someList">
            <list>
                <value>first</value>
                <value>second</value>
            </list>
        </property>

        <!-- 3. map → Map -->
        <property name="someMap">
            <map>
                <entry key="an entry" value="just some string"/>
                <entry key="another" value="another value"/>
            </map>
        </property>

        <!-- 4. set → Set -->
        <property name="someSet">
            <set>
                <value>just some string</value>
                <value>another element</value>
            </set>
        </property>

        <!-- 5. 复杂 Map：value 可为 list、null -->
        <property name="complexMap">
            <map>
                <entry key="tags">
                    <list>
                        <value>java</value>
                        <value>spring</value>
                        <value>xml</value>
                    </list>
                </entry>
                <entry key="optional">
                    <null/>
                </entry>
            </map>
        </property>

        <!-- 6. 复杂 List：元素可为 map -->
        <property name="complexList">
            <list>
                <map>
                    <entry key="name" value="Alice"/>
                    <entry key="age" value="25"/>
                </map>
                <map>
                    <entry key="name" value="Bob"/>
                    <entry key="age" value="30"/>
                </map>
            </list>
        </property>

        <!-- 7. 强类型 Map<String, Float>，XML 字符串自动转为 Float -->
        <property name="accounts">
            <map>
                <entry key="one" value="9.99"/>
                <entry key="two" value="2.75"/>
                <entry key="six" value="3.99"/>
            </map>
        </property>
    </bean>

</beans>
